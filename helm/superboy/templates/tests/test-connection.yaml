apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "superboy.fullname" . }}-test-connection"
  labels:
    {{- include "superboy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "superboy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
